<!DOCTYPE html>
<meta http-equiv="cache-control" content="no-cache" />
<html>
<body>

<h1>User "<%= user_id %>".</h1>

<img src="/image/<%= user_id %>" onerror="this.style.display='none'"/><br>

<p>Upload a new image:</p>
<form method="post" action="/d/<%= user_id %>"
      ref="uploadForm" id="uploadForm"
      encType="multipart/form-data">
  <input type="file" name="map" accept="image/*">
  <input type="submit">
</form>

</body>
</html>
